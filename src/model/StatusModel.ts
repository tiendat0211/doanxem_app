export interface StatusModel {
  post_id: number,
  user_img: any,
  user_name: string,
  time: string,
  status_content: string,
  status_img: any,
  comment_counts: string,
  reaction_counts: string,
}
